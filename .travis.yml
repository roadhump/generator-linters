language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-linters' ]; then cd .. && eval "mv $currentfolder generator-linters" && cd generator-linters; fi
